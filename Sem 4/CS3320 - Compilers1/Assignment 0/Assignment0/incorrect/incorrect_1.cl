class Main {
    x : Int;
    fi : String;     -- Identifiers are strings other than keywords(here fi is a keyword)
    str : string;    -- Type identifiers begin with capital letter(s in string has to be capital)
    Sum : Int;       -- Object identifiers begin with lower case letter(S in Sum has to be lowercase)

    main() : Object {
        x <- 1025e69d          -- Integers are strings of digits 0-9(e and d are not digits)
    };
};