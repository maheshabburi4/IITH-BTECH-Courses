I Named the c file as "Calc-CS18BTECH11001.c" 
Opened terminal in the same directory where the c file is saved,
compilation command : gcc -o calc Calc-CS18BTECH11001.c 
working : compiles the given code and creats a "application/x-sharedlib" file in the same directory where the c file is saved with name "calc" which is used for running the code.
Run command : ./calc
working : takes three inputs from the user(1 character,2 integers) and prints the output on the terminal.